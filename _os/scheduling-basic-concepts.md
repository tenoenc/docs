### 기본 개념

* **CPU-I/O Burst Cycle:** 프로세스는 CPU를 쓰는 시간(CPU Burst)과 I/O를 기다리는 시간(I/O Burst)을 번갈아 수행함.
* **스케줄러 (CPU Scheduler)와 디스패처 (Dispatcher):** 단기 스케줄러의 역할.
* **선점형(Preemptive) vs 비선점형(Non-preemptive) 스케줄링:** 현대 OS는 대부분 선점형.